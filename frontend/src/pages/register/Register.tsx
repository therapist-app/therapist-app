import { Typography } from "@mui/material";

const Register = () => {
  return (
    <div>
      <Typography variant="h3">This is the register page</Typography>
    </div>
  );
};

export default Register;
